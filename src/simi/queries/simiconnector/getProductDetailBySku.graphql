#import "../Fragments/ProductDetail"
query productDetail($sku: String, $onServer: Boolean!) {
    productDetail: products(filter: { sku: { eq: $sku } }) {
        items {
            ... ProductDetail
        }
    }
  	simiProductDetailExtraField(filter:  { sku: { eq: $sku }})
}
